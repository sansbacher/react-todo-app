import React from 'react'

import Logo from './Logo'

const Instructions = () => {
	return (
		<Logo message="Enter anything you need to do, when you're done check it off!" />
	);
}

export default Instructions;